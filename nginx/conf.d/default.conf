# Upstream backend server
upstream backend {
    server backend:5000 max_fails=3 fail_timeout=30s;
    keepalive 32;
}

# Upstream frontend server
upstream frontend {
    server frontend:3000 max_fails=3 fail_timeout=30s;
    keepalive 32;
}

# HTTP Server - Redirect to HTTPS (except for ACME challenge)
server {
    listen 80;
    server_name _;

    # Logging
    access_log /var/log/nginx/weather_access.log;
    error_log /var/log/nginx/weather_error.log;

    # ACME Challenge for Let's Encrypt - MUST be accessible via HTTP
    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Health check endpoint
    location /health {
        access_log off;
        return 200 "OK\n";
        add_header Content-Type text/plain;
    }

    # Redirect all other HTTP traffic to HTTPS
    location / {
        return 301 https://$host$request_uri;
    }
}

# HTTPS Server moved to conf.d/ssl.conf.template and enabled dynamically by entrypoint
